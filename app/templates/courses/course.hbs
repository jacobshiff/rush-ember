<h1>{{model.studio.name}}</h1>
<h3>{{model.title}}</h3>
<h4>
  Instructor: {{model.instructor}} | Categories:
  {{#each model.category_tags as |cat|}}
    {{cat}}
  {{/each}}
</h4>
<p>
  Description:
  {{model.description}}
</p>
{{outlet}}
