<div class="row">
  <div class="col-xs-4">
  All. The. Studios.
    <ul>
      {{#each model as |studio|}}
        <li class="source-name">{{link-to studio.name 'studios.studio' studio}}</li>

      {{/each}}
    </ul>
  </div>
  <div class="col-xs-8">
    {{outlet}}
  </div>
</div>
